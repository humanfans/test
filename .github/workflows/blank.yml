name: linyaps

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: 🔧 添加 Linglong APT 源
        run:  'wget https://gitee.com/LFRon/Linyaps-generic-linux-SIG/releases/download/1.9.12/ubuntu-24.04-x86_64.tar.gz'
      - name: 🔧 添加 Linglong APT 源
        run: 'tar -zxvf ubuntu-24.04-x86_64.tar.gz'
     # - name: 📦 解压
        #run: sh ./install.sh
      - name: 📦 更新并安装 Linglong 工具
        run: sudo apt install ./*deb
      - name: ✅ 验证安装
        run: apt policy linglong-bin
